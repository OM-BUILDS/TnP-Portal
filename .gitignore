# Dependency directories
node_modules/
frontend/node_modules/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build output
frontend/dist
frontend/build

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Temporary files created by express-fileupload
/tmp/

# Editor directories and files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Operating System generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
# Ignore all files and directories by default
/*

# Whitelist the specific directories you want to keep.
# The trailing slash is important to specify they are directories.
!/Backend_latest version/
!/Frontend_latest version/

# Whitelist the .gitignore file itself so it gets committed to the repository.
!/.gitignore

# You can add rules for files inside the whitelisted directories below.
# For example, to ignore node_modules inside both directories:
Backend_latest version/node_modules/
Frontend_latest version/node_modules/

# To ignore .env files inside both directories:
Backend_latest version/.env
Backend_latest version/backend/uploads
Backend_latest version/backend/config/config.env
Frontend_latest version/frontend/dist
Frontend_latest version/.env
